# Анализ влияния сносок на статистику

## ⚠️ ВАЖНО: Сноски УЧИТЫВАЮТСЯ в анализе!

### Результаты проверки:

**Да, сноски попадают в статистическую выборку!**

Программа использует паттерн `r"(?ui)[а-яё]+"`, который извлекает **все** кириллические слова из текста, включая слова из сносок и примечаний.

### Статистика из вашего файла:

- **Найдено сносок с текстом**: 58 вхождений
- **Всего слов из сносок**: 112 слов
- **Общее количество слов во всех стихах**: 97,733 слов
- **Доля сносок**: 0.11% от общего объема

### Типы сносок, которые учитываются:

1. **Языковые пометки**: `(польск.)`, `(лат.)`, `(фр.)`, `(нем.)`, `(англ.)` и т.д.
   - Пример: `Польша не погибла! (польск.)` → учитывается как 4 слова: `Польша`, `не`, `погибла`, `польск`

2. **Примечания в скобках**: `(отрывок)`, `(сказка)`, `(притча)`, `(Кантата)`
   - Пример: `(Отрывок из поэмы)` → учитывается как 3 слова: `Отрывок`, `из`, `поэмы`

3. **Отдельные строки-примечания**: строки, состоящие только из текста в скобках
   - Пример: `(Подражание французскому)` → учитывается как 2 слова

4. **Ссылки**: `(см. стр. 5)`, `(прим. ред.)`
   - Пример: `(см. стр. 5)` → учитывается как 2 слова: `см`, `стр`

### Примеры из вашего файла:

```
Строка 22368: Польша не погибла! (польск.)
→ Учитывается: ['Польша', 'не', 'погибла', 'польск'] (4 слова)

Строка 4746: (отрывок)
→ Учитывается: ['отрывок'] (1 слово)

Строка 2966: Наполеон на Эльбе (1815)
→ Учитывается: ['Наполеон', 'на', 'Эльбе'] (3 слова)
(цифра 1815 не учитывается)
```

### Проблемные случаи:

1. **Сноски между разделителями**: Сноска `Польша не погибла! (польск.)` находится между `======`, 
   что означает, что она считается частью стихотворения "Родриг". Это **неправильно** - это сноска, а не часть стиха.

2. **Номера сносок**: Строки типа `91` (номер сноски) не учитываются (только цифры), 
   но текст сносок учитывается.

### Рекомендации:

#### Вариант 1: Удалить сноски перед анализом (рекомендуется)

Если вы хотите получить "чистую" статистику без сносок, можно:

1. Удалить все строки, состоящие только из текста в скобках: `^(\([^)]+\))$`
2. Удалить языковые пометки в конце строк: `\([а-яё]+\.\)$`
3. Удалить примечания в скобках: `\([а-яё]+\)$`
4. Удалить ссылки: `\(см\.|\(прим\.`

#### Вариант 2: Оставить как есть

Если сноски составляют незначительную долю (0.11%), их влияние на общую статистику минимально. 
Однако для точного анализа авторского стиля лучше их удалить.

### Влияние на анализ:

- **Энтропия**: минимальное влияние (0.11% слов)
- **Частотный анализ**: слова типа `польск`, `лат`, `отрывок` будут иметь небольшую частоту
- **Авторский стиль**: сноски не являются частью авторского текста, поэтому могут исказить анализ

### Вывод:

**Сноски учитываются в анализе**, но их доля невелика (0.11%). 
Для максимальной точности анализа рекомендуется удалить сноски перед обработкой.

